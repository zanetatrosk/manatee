// Purpose: Contains all the paths for the application.

const PATHS = {
    CHARACTERS: "/characters",
    CREATE_CHARACTER: "/characters/create-character",
    CHARACTER_SHEET: '/characters/character-sheet',
    CONTENT: "/content",
};

Object.freeze(PATHS);
export default PATHS;